# Regenerated by `node scrape.js` on every run, ~6 MB and different every time —
# committing it would bloat the repo for data that has a shelf life of hours.
results.json

# Build outputs.
dist/
sync.sql

node_modules/
.DS_Store
.wrangler/

# Local tooling config, not part of the project.
.claude/
